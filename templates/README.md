# Built-in templates

Templates are JSON configuration, not business logic. A template selects an Entry type and creates its Section/Facet layout, including an unnamed Facet for a Section whose tags need no visible Facet label. Templates never create Collections or generic Field Definitions.

# CLI scripts

`db-probe.ts`, `db-doctor.ts`, `db-benchmark.ts`, and `export-demo.ts` are introduced at their corresponding working milestones; commands must not claim PASS before their behavior exists.

# Database scenarios

Probe scenarios create deterministic Entry types, Section/Facet layouts, Producers, separate tag vocabularies, and Content rows. Tests must use in-memory or temporary databases and never target `.data/library.db`.
